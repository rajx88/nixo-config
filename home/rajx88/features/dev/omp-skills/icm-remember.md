---
name: icm-remember
description: >
  Store information in ICM persistent memory. Use when user says "/remember", "icm remember",
  "save this", "store this in memory", or wants to persist context across sessions.
---

Store the following in ICM memory: $ARGUMENTS

Run:
```bash
icm remember "$ARGUMENTS"
```
