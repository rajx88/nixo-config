{
  wayland.windowManager.hyprland.settings = {
    windowrule = [
      # JetBrains IDEs: keep focus on floating dialogs (Find in Files, etc.)
      # "stay_focused on, match:class jetbrains-.*, match:float true"
    ];
  };
}
